
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0koa7o9":  
    rospy.loginfo("Task reach motor with the ID: activity_0koa7o9 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task reach motor with the ID activity_0koa7o9 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_15ti3ns":  
    rospy.loginfo("Task move motor with the ID: activity_15ti3ns in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor with the ID activity_15ti3ns has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_15ti3ns":  
    rospy.loginfo("Task move motor with the ID: activity_15ti3ns in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor with the ID activity_15ti3ns has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0ebmhpg":  
    rospy.loginfo("Task grasp motor with the ID: activity_0ebmhpg in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor with the ID activity_0ebmhpg has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0ebmhpg":  
    rospy.loginfo("Task grasp motor with the ID: activity_0ebmhpg in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor with the ID activity_0ebmhpg has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0vzrvtp":  
    rospy.loginfo("Task move motor assembly0 and motor with the ID: activity_0vzrvtp in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly0 and motor with the ID activity_0vzrvtp has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0vzrvtp":  
    rospy.loginfo("Task move motor assembly0 and motor with the ID: activity_0vzrvtp in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly0 and motor with the ID activity_0vzrvtp has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_068759n":  
    rospy.loginfo("Task release motor assembly0 and motor with the ID: activity_068759n in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly0 and motor with the ID activity_068759n has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_068759n":  
    rospy.loginfo("Task release motor assembly0 and motor with the ID: activity_068759n in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly0 and motor with the ID activity_068759n has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_068759n":  
    rospy.loginfo("Task release motor assembly0 and motor with the ID: activity_068759n in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly0 and motor with the ID activity_068759n has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    