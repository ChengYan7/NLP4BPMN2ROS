
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_11fiqbb":  
    rospy.loginfo("Task reach base plate with the ID: activity_11fiqbb in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task reach base plate with the ID activity_11fiqbb has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1uwgc5r":  
    rospy.loginfo("Task grasp baseplate with the ID: activity_1uwgc5r in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp baseplate with the ID activity_1uwgc5r has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1uwgc5r":  
    rospy.loginfo("Task grasp baseplate with the ID: activity_1uwgc5r in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp baseplate with the ID activity_1uwgc5r has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_03afyx2":  
    rospy.loginfo("Task move baseplate with the ID: activity_03afyx2 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move baseplate with the ID activity_03afyx2 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_03afyx2":  
    rospy.loginfo("Task move baseplate with the ID: activity_03afyx2 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move baseplate with the ID activity_03afyx2 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0cuizk7":  
    rospy.loginfo("Task release baseplate with the ID: activity_0cuizk7 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release baseplate with the ID activity_0cuizk7 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0cuizk7":  
    rospy.loginfo("Task release baseplate with the ID: activity_0cuizk7 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release baseplate with the ID activity_0cuizk7 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_007qjvu":  
    rospy.loginfo("Task reach motorholder with the ID: activity_007qjvu in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task reach motorholder with the ID activity_007qjvu has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1i5rkzm":  
    rospy.loginfo("Task move motor holder with the ID: activity_1i5rkzm in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor holder with the ID activity_1i5rkzm has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1i5rkzm":  
    rospy.loginfo("Task move motor holder with the ID: activity_1i5rkzm in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor holder with the ID activity_1i5rkzm has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0a2t5t0":  
    rospy.loginfo("Task grasp motor holder with the ID: activity_0a2t5t0 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor holder with the ID activity_0a2t5t0 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0a2t5t0":  
    rospy.loginfo("Task grasp motor holder with the ID: activity_0a2t5t0 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor holder with the ID activity_0a2t5t0 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0ebqvso":  
    rospy.loginfo("Task move transmission gear and motor holder with the ID: activity_0ebqvso in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move transmission gear and motor holder with the ID activity_0ebqvso has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0ebqvso":  
    rospy.loginfo("Task move transmission gear and motor holder with the ID: activity_0ebqvso in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move transmission gear and motor holder with the ID activity_0ebqvso has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0ebqvso":  
    rospy.loginfo("Task move transmission gear and motor holder with the ID: activity_0ebqvso in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move transmission gear and motor holder with the ID activity_0ebqvso has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1vv7b7o":  
    rospy.loginfo("Task release transmission gear and motor holder with the ID: activity_1vv7b7o in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release transmission gear and motor holder with the ID activity_1vv7b7o has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1vv7b7o":  
    rospy.loginfo("Task release transmission gear and motor holder with the ID: activity_1vv7b7o in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release transmission gear and motor holder with the ID activity_1vv7b7o has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1vv7b7o":  
    rospy.loginfo("Task release transmission gear and motor holder with the ID: activity_1vv7b7o in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release transmission gear and motor holder with the ID activity_1vv7b7o has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0eh4xhq":  
    rospy.loginfo("Task reach motor with the ID: activity_0eh4xhq in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task reach motor with the ID activity_0eh4xhq has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1q6l3tk":  
    rospy.loginfo("Task move motor with the ID: activity_1q6l3tk in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor with the ID activity_1q6l3tk has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1q6l3tk":  
    rospy.loginfo("Task move motor with the ID: activity_1q6l3tk in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor with the ID activity_1q6l3tk has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0gs1quv":  
    rospy.loginfo("Task grasp motor with the ID: activity_0gs1quv in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor with the ID activity_0gs1quv has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0gs1quv":  
    rospy.loginfo("Task grasp motor with the ID: activity_0gs1quv in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor with the ID activity_0gs1quv has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1rrxkmv":  
    rospy.loginfo("Task move motor assembly0 and motor with the ID: activity_1rrxkmv in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly0 and motor with the ID activity_1rrxkmv has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1rrxkmv":  
    rospy.loginfo("Task move motor assembly0 and motor with the ID: activity_1rrxkmv in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly0 and motor with the ID activity_1rrxkmv has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1rrxkmv":  
    rospy.loginfo("Task move motor assembly0 and motor with the ID: activity_1rrxkmv in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly0 and motor with the ID activity_1rrxkmv has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0szgfm1":  
    rospy.loginfo("Task release motor assembly0 and motor with the ID: activity_0szgfm1 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly0 and motor with the ID activity_0szgfm1 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0szgfm1":  
    rospy.loginfo("Task release motor assembly0 and motor with the ID: activity_0szgfm1 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly0 and motor with the ID activity_0szgfm1 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0szgfm1":  
    rospy.loginfo("Task release motor assembly0 and motor with the ID: activity_0szgfm1 in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly0 and motor with the ID activity_0szgfm1 has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1v4qvbr":  
    rospy.loginfo("Task reach motor with the ID: activity_1v4qvbr in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task reach motor with the ID activity_1v4qvbr has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1b2mptw":  
    rospy.loginfo("Task grasp motor assembly1 with the ID: activity_1b2mptw in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor assembly1 with the ID activity_1b2mptw has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1b2mptw":  
    rospy.loginfo("Task grasp motor assembly1 with the ID: activity_1b2mptw in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp motor assembly1 with the ID activity_1b2mptw has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0brjnhs":  
    rospy.loginfo("Task move motor assembly1 and motor gear with the ID: activity_0brjnhs in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly1 and motor gear with the ID activity_0brjnhs has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0brjnhs":  
    rospy.loginfo("Task move motor assembly1 and motor gear with the ID: activity_0brjnhs in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly1 and motor gear with the ID activity_0brjnhs has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0brjnhs":  
    rospy.loginfo("Task move motor assembly1 and motor gear with the ID: activity_0brjnhs in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move motor assembly1 and motor gear with the ID activity_0brjnhs has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1xt83aa":  
    rospy.loginfo("Task release motor assembly1 and motor gear with the ID: activity_1xt83aa in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly1 and motor gear with the ID activity_1xt83aa has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1xt83aa":  
    rospy.loginfo("Task release motor assembly1 and motor gear with the ID: activity_1xt83aa in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly1 and motor gear with the ID activity_1xt83aa has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1xt83aa":  
    rospy.loginfo("Task release motor assembly1 and motor gear with the ID: activity_1xt83aa in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release motor assembly1 and motor gear with the ID activity_1xt83aa has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_05aav5x":  
    rospy.loginfo("Task reach baseplate with the ID: activity_05aav5x in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task reach baseplate with the ID activity_05aav5x has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0of4w8h":  
    rospy.loginfo("Task grasp base plate with the ID: activity_0of4w8h in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp base plate with the ID activity_0of4w8h has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0of4w8h":  
    rospy.loginfo("Task grasp base plate with the ID: activity_0of4w8h in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task grasp base plate with the ID activity_0of4w8h has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0aua5ph":  
    rospy.loginfo("Task move base plate with the ID: activity_0aua5ph in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move base plate with the ID activity_0aua5ph has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_0aua5ph":  
    rospy.loginfo("Task move base plate with the ID: activity_0aua5ph in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task move base plate with the ID activity_0aua5ph has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1bcv7ph":  
    rospy.loginfo("Task release base plate with the ID: activity_1bcv7ph in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release base plate with the ID activity_1bcv7ph has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    
if taskDefinition["object"] == "urn:ngsi-ld:TaskDefinition:siemens:Activityactivity_1bcv7ph":  
    rospy.loginfo("Task release base plate with the ID: activity_1bcv7ph in progress.")
    # Here goes the action calling for the precise task definition
    Activity_name = taskDefinition["object"][-16:]
    Activity = activity(Activity_name)
    # Sends the goal to the action server.
    self.activeActivity = Activity
    rospy.loginfo("Task release base plate with the ID activity_1bcv7ph has been executed.")
    taskStatus["value"] = "inProgress"
    taskStatus["observedAt"] = datetime.now().strftime("%Y-%m-%dT%H:%M:%SZ") 
    