Python folder contains the BPMN parser which extracts the relevant activities in from the BPMN diagram,
and dictionary used to perform the pre-processing of the extracted elements.

BMPN_files folder is a source folder for the BPMN diagrams.